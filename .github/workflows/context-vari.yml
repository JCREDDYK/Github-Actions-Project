name: Context variable Demo
on:
  workflow_dispatch: 
jobs:
  context-demo:
    runs-on: ubuntu-latest
    steps:
      - name: Use contex variables
        run: |
          echo "The workflow is triggered by ${{ github.actor }}"
          echo "The repository is ${{ github.repository }}"
      - name: print all variables undr github context
        run: echo "${ { toJson(github) } }"
